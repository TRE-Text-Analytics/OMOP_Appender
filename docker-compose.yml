services:
  omop-appender:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Optional: set defaults via env vars (can still be overridden in the UI)
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
